# Feuille de route — AppProspection

> Ordre de priorité convenu. ✅ fait · 🔵 en cours · ⬜ à venir.

## Fondations ✅
- [x] Cadrage produit + spec (`SPEC.md`)
- [x] Choix techniques (PWA + Supabase + MapLibre/IGN/BAN)
- [x] Schéma de base de données (`db/schema.sql`)
- [x] Échafaudage PWA + carte IGN
- [x] Auth + persistance des points + temps réel (Supabase)
- [x] Détail / édition d'un point (clic sur marqueur)
- [x] Fond de carte **vectoriel** (Plan IGN) — rendu net et moderne

## Fondations (suite) ✅
- [x] **Recherche d'adresse (géocodage BAN)** — barre de recherche + centrage carte
- [x] **Affichage des bâtiments en 3D** — bouton 2D/3D, extrusion de `bati_surf` (champ `hauteur`) du Plan IGN, masquée en mode Toits

## Fondations (suite) ✅
- [x] **Agenda + flux RDV** — poser « RDV pris » ouvre la saisie du RDV (date/client/adresse auto via reverse BAN) ; agenda partagé (couleur par commercial) ; issues en 1 clic (Vendu/Effectué/Manqué/Annulé) ; « Vendu » rebascule le point en vendu. *Proposition à valider avec l'ami (Q6/Q7/Q10).*
- [x] **Statistiques (manager)** — tunnel de conversion (portes→contacts→RDV→effectués→ventes) + taux, classement des commerciaux, objectif hebdo de RDV (éditable par le manager), sélecteur jour/semaine/mois. *Définitions à valider avec l'ami (Q10/Q11).*

## Visuel carte (« beau & lisible »)
- [x] **Lisibilité** — fond adouci (voile sous les labels), marqueurs à icônes par statut (ombre, halo de sélection), regroupement/clustering avec bulles cliquables
- [x] **Effet 3D waouh** — éclairage des bâtiments, surbrillance (bleu) de la maison sélectionnée (ciel/brouillard retirés, invisibles). ~~Bâtiments sombres~~ → **bâtiments clairs** (réf. Apple Plans, cohérent DA) depuis le lot visuel de juillet 2026. **→ RETIRÉE (fin juillet 2026, décision briac)** : les prismes à toit plat (LOD1) n'apportaient rien sur le terrain et juraient avec les pans dessinés ; remplacée par la **maquette 3D du toit** dans la fiche (voir section LiDAR).
- [x] **Vue Toits hybride** — ortho-photo + noms de rues par-dessus (bâtiments blancs du plan masqués). **Vue Toits par défaut** à l'ouverture. (Contours des bâtiments testés puis retirés.)
- [x] **Vue rue / immersion** — explorée puis **ABANDONNÉE** : Mapillary trop juste en pavillonnaire, Google Street View écarté (dépendance + facturation), 3D photoréaliste Google bloquée en France. Code retiré.

## Fiabilité carte — suite de l'audit (juillet 2026)
- [x] **Lot 1 — Fiabilité de la donnée** : tolérance de tap (±14 px, plus de point parasite à côté d'un marqueur), garde double-tap-zoom (pose différée 300 ms, annulée par le 2e tap), toast « Point posé » avec action **Annuler**, pose **optimiste** (le point apparaît immédiatement, rollback + toast d'erreur si l'insert échoue).
- [x] **Lot 2 — Lisibilité soleil + cohérence DA** : halo blanc des labels uniquement quand l'ortho est active (restauré en plan), statuts « absent »/« à revoir » assombris (contraste ≥ 3:1 avec le glyphe blanc, alignés `status.ts` ↔ `--st-*`), glyphes et anneau des marqueurs épaissis, cluster blanc à anneau accent (fini la confusion avec « impossible »), halo/bâtiment sélection sur `--accent`, contrôles MapLibre restylés (tokens + cibles 44 px), Toits ↔ 3D cohérents (3D désactivée en vue Toits).
- [x] **Lot visuel (retour captures d'écran)** : bâtiments 3D passés de l'anthracite au **clair** (la masse sombre écrasait la vue), cercle de précision de la géolocalisation retiré (gros halo bleu laiteux), voile papier renforcé et placé **sous les routes** (le réseau de rues blanc ressort enfin), teintes eau/végétation ajustées, ortho ravivée (saturation/contraste), marqueurs qui grossissent jusqu'au zoom 19.
- [x] **Lot 3a — Carte persistante** : la carte reste montée entre les onglets (masquée en `visibility`), retour instantané, position/zoom conservés, pas de re-téléchargement des tuiles. Quitter l'onglet ferme drawers et mode visée.
- [x] **Lot 3b — Pose au réticule** (pattern Badger/SalesRabbit) : FAB « + » (zone pouce) → réticule au centre, on déplace la carte sous le viseur → statut + « Poser ici ». La pose au tap est retirée (plus de point accidentel possible, le doigt ne masque plus la maison). Garde-fou : zoom ≥ 15 requis.
- [x] **Lot 3c — Sheet détail non modale** : la carte reste visible et manipulable derrière le détail d'un point ; le point sélectionné est recadré au-dessus de la sheet (`easeTo` + padding bas, rendu à la fermeture) ; le halo de sélection prend la couleur du statut.
- [x] **Lot 3d — Clusters** : d'abord en donut par statut, puis **simplifiés après retour de briac** en badge accent (cercle bleu DA, chiffre blanc Geist Mono, halo doux — style pin Airbnb) : plus smooth, plus pro. Seuils abaissés (`clusterMaxZoom` 13, rayon 36) : dès l'échelle quartier (z14+) on voit tous les points individuellement, les bulles ne restent qu'aux échelles ville. Marqueurs DOM (`config/clusters.ts`), clic = zoom.
- [x] **Vol de caméra corrigé (juillet 2026, retour briac)** : chercher une adresse puis se faire ramener à sa position — le contrôle de géolocalisation était en mode **suivi continu** (re-centrage à chaque fix GPS, insensible aux déplacements programmatiques). Passé en localisation **à la demande** (one-shot, zoom rue) ; le zoom d'ouverture sur sa position ne s'exécute plus que si l'utilisateur n'a pas déjà navigué (fin de la course au chargement). Audit des autres mouvements de caméra (clusters, recadrage sheet, flyTo agenda/recherche) : cohérents.
- [x] **Filtre carte par ancienneté (retour briac, fin juillet 2026)** : 2e rangée de chips dans la barre de filtres (« > 2 sem / > 1 mois / > 3 mois »), combinée en ET avec les statuts — ex. « Absent » + « > 1 mois » = les portes à retenter en priorité. Basé sur la nouvelle colonne `visited_at` (dernière visite commerciale : pose ou changement de statut — PAS les écritures techniques de cache qui polluent `updated_at`), backfillée depuis `point_events` (**migration `db/0012_visited_at.sql` à exécuter**).
- [ ] ⬜ **Lot 3 (suite, à choisir)** : filtre carte par commercial.

## Système de notes (audit juillet 2026 — plainte : notes invisibles dans l'agenda)
- [x] **Quick wins notes** : note du RDV affichée sur la carte agenda (tous statuts) + note terrain du point lié (jointure) + bouton « Carte » (bascule d'onglet + flyTo + sélection) ; Modifier/Supprimer accessibles quel que soit le statut du RDV ; champ « Note (facultatif) » dans la barre de visée (capture à chaud, écrite dans `points.notes` ET `point_events.note`) ; pastille « a une note » sur les marqueurs ; fin du faux succès (erreur RLS/réseau → toast d'erreur) ; note éditable même si le fetch détail échoue ; note du point affichée dans le formulaire RDV.
- [x] **Flux de pose clarifié (retour briac)** : le champ note de la barre de visée est retiré (il laissait croire que c'était la seule saisie possible) ; désormais **chaque pose ouvre une fiche** — formulaire RDV complet pour « RDV pris », fiche du point (statut, **client**, note) pour les autres statuts. Nouvelle colonne `points.client_name` (**migration `db/0003_point_client_name.sql` à exécuter dans Supabase**).
- [x] **Journal de notes + client unifié** : table dédiée `point_notes` (**migration `db/0004_point_notes.sql` à exécuter** — séparée de `point_events` pour ne pas fausser les stats ; reprend en historique les notes existantes des points ET des RDV liés). Fiche point : historique horodaté avec auteur + champ « Ajouter une note » (on n'écrase plus jamais). Le nom du client saisi dans un RDV est synchronisé sur le point ; la note d'un RDV créé rejoint aussi le journal de la maison. `points.notes` = dernière note (pastille, agenda).

## Design — DA « Clair & précis » (Linear/Vercel/Emil Kowalski)
- [x] Système de design (tokens, police Geist, icônes Lucide, Sonner, Vaul, Motion)
- [x] Barre de navigation + écran Accueil
- [x] Carte (barre d'outils, recherche, chips, drawers Vaul, toasts)
- [x] Agenda (vue calendrier mois + planning du jour)
- [x] Stats (segmented animé, tunnel, classement)
- [ ] ⬜ Écran de connexion (encore ancien style) + finitions (contrôles carte)

## Déploiement ✅
- [x] Render (Static Site via `render.yaml`), repo GitHub `autonome-ia/appprospection`, HTTPS + PWA installable. Redéploiement auto à chaque `git push`.

## Quick wins SalesRabbit (étude `etude-salesrabbit.md`, juillet 2026)
- [x] **Relances datées** : champ « Revoir le » sur les points « à revoir » (**migration `db/0005_revisit_at.sql` à exécuter**), bloc « À relancer » sur l'Accueil (clic → carte + fiche). La date s'efface si le statut change.
- [x] **Adresse automatique** : géocodage inverse BAN en arrière-plan à la pose → adresse sur la fiche, l'Accueil et l'agenda.
- [x] **Filtres carte par statut** : chips en bas de carte (multi-sélection, vide = tout). « Vendu » seul = voir les chantiers pour prospecter autour (Customer Locator de SalesRabbit).
- [x] **Feed d'activité** sur l'Accueil : dernières actions de l'équipe (auteur, statut, client/adresse, il y a X min) depuis `point_events`.
- [x] **Retours briac** : les « à revoir » datés apparaissent aussi dans l'**agenda** (pastille ambre dans la grille du mois + liste « à revoir » sous les RDV du jour, tap → carte) ; les chips de filtre sont **repliées derrière un bouton filtres** dans la barre d'outils (la carte reste dégagée, bouton surligné quand un filtre est actif).
- [x] **Fiche maison enrichie** (étude `etude-donnees-maisons.md`, plan validé par briac) : à la pose, 2 appels open data en arrière-plan — WFS BD TOPO (matériau de toiture + altitudes → **surface de toit estimée**, calcul emprise/cos(pente) en Lambert-93) et BDNB (**année de construction** + classe DPE). Cache définitif sur le point (**migration `db/0006_maison_enrichie.sql` à exécuter**), backfill paresseux à l'ouverture des fiches anciennes. Affichage : badges compacts sous l'en-tête de la fiche (`~1972 · Ardoise · ~120 m² toit · DPE E`) avec nuances (~, « probable », title explicatifs) + attribution IGN/CSTB. Module `data/enrich.ts` (proj4) en chunk séparé chargé à la demande. Coût : 0 €.
- [x] **Fiche maison AVANT prospection (retour briac)** : taper une maison sans marqueur (zoom ≥ 16, garde double-tap) ouvre une sheet non modale — adresse, badges maison, **maison surlignée** sur la carte (polygone IGN) — avec choix du statut + « Poser le point » (enchaîne fiche/RDV comme le réticule). Consultation sans trace ; cache mémoire par coordonnées (poser après consultation ne refait aucun appel, ménage le quota BDNB).
- [x] **Précision toiture (retour briac : « Autre » trop vague)** : la nomenclature fiscale n'a que 5 catégories (limite de la source, pas un réglage) — matériau **secondaire** affiché (« Tuiles + zinc/alu ») et champ **« Toiture constatée »** sur la fiche (liste métier avec ardoise naturelle vs ardoise fibrociment, **migration `db/0007_mat_toit_confirme.sql`** exécutée) qui remplace la donnée fiscale — badge bleu « confirmé ». Un signal automatique « fibro ? » (Autre + ≤ 1997) a été testé puis **retiré** (jugé non pertinent : trop de bruit sur le parc ancien breton). Liste des matériaux à affiner avec le chef des ventes.
- [ ] ⬜ **Territoires** (en attente du retour du chef des ventes) : polygones dessinés par le manager + assignation + % de couverture (avec le nb réel de maisons par zone via BD TOPO). Puis équipe/invitations avant la prod.

## Vue satellite plus belle (étude `etude-imagerie-satellite.md`, juillet 2026)
- [x] **Étape 1 — IGN au maximum réel (gratuit)** : `maxzoom` 21→19 (zoom natif réel depuis que l'IGN a retiré la THR en mars 2025 — on demandait des tuiles inexistantes au zoom maison) ; **photo pure en mode Toits** : le voile papier `base-wash` et ~370 tracés du plan (routes en rubans blancs/jaunes, plans d'eau en aplats) étaient dessinés PAR-DESSUS l'ortho → masqués en Toits, restaurés en plan (seuls les noms de rues restent sur la photo) ; paint ortho adouci, dégressif avec le zoom ; variante « retina » WMS 512 px testable via `?ortho=wms2x` (A/B — colorimétrie du niveau pyramide à valider à l'écran).
- [ ] ⬜ **Étape 2 — POC Mapbox Satellite** (source raster dans MapLibre, autorisé officiellement, 750k tuiles/mois gratuites, @2x natif) : briac crée un token public gratuit → `VITE_MAPBOX_TOKEN` → comparaison côte à côte sur les zones prospectées, verdict du chef des ventes. Repli : Google 2D Tiles. MapKit JS non écarté mais dernier recours (réécriture complète, gain non démontré en pavillonnaire breton).

## Mesure de toiture LiDAR HD (SOP `sop-mesure-toiture-lidar.md`, juillet 2026)
- [x] **Cadrage + fondations vérifiées** : surface de toit quasi réelle (±3-5 % visés) depuis le nuage de points LiDAR HD IGN (open data, 0 €), en fond de fiche maison — pas de nouvelle carte. Dalle de test (Lesneven) trouvée via WFS, acquisition 12/2024, format COPC streamable (Range 206 testé). Phases avec GO/NO-GO dans le SOP.
- [x] **Phase 0 — spike technique** (`tools/lidar-spike/`) : pipeline complet fonctionnel sur 3 maisons de Lesneven (streaming COPC 2 Mo/maison en 3-6 s, pans + pentes + azimuts cohérents, cas « 2 dalles » résolu). Mesures +15 à +48 % vs estimation actuelle (débords + pentes réelles).
- [x] **Validation interne (sans vérité terrain)** : banc synthétique (`bench.mjs`, vérité mathématique, même code de mesure) → **erreur max 5,7 %** ; test toits plats réels (surface = emprise connue) → bug de double comptage multi-pans trouvé et corrigé (dédup des cellules).
- [ ] ⬜ **Gate G0 (précision externe)** : comparer sur 3-4 maisons de surface réellement connue — surfaces facturées du chef des ventes, ou auto-test briac (plans / facture / mesure pignon à l'inclinomètre).
- [x] **Phase 1 — durcissement (cœur)** : RANSAC déterministe, seuils adaptatifs à la densité, fusion des pans sur-segmentés, fermeture morphologique (banc à ±1,9 %), exclusion des mitoyens, ventilation par pan (principal/plat/secondaire → `totalPrincipal` pour le couvreur), gestion du rate-limit IGN (429 → concurrence 4 + backoff). Médiane vs cadastre Lyon : 15 % (l'estimation actuelle fait 34 % au même étalon).
- [x] **Gate G1 : PASSÉ** (`g1-suite.mjs`, 21 bâtiments, 4 scénarios) : pavillons 9,1 % / toits plats 12,6 % / mitoyens 8,7 % / boisés 16,3 % de médiane vs cadastre Lyon ; zéro crash — les cas pathologiques rendent des **verdicts** (`no_data`, `faible_confiance`, `grand_batiment`) au lieu de mentir ; végétation robuste (arbres jusqu'à 302 % de points sans dégrader la mesure).
- [x] **Phase 2 — intégration app** : mesure **dans le navigateur** (CORS IGN vérifié ouvert — pas d'Edge Function nécessaire, zéro infra), chunk séparé `data/lidar.ts` (copc + laz-perf wasm ~320 Ko chargés à la première mesure), déclenchée à la pose + backfill paresseux à l'ouverture des fiches anciennes, cache définitif en base avec **version d'algo** (un recalibrage post-G0 re-mesure tout paresseusement). Badge fiche : `137 m² toit` (style « confirmé », sans ~) quand le verdict est `ok`, sinon l'estimation reste affichée. **Migration `db/0008_toit_lidar.sql` À EXÉCUTER dans Supabase AVANT de tester** (sinon la carte ne charge plus les points).
- [x] **Gate G0 : VALIDÉ par le chef des ventes** (test en conditions réelles sur ses chantiers — GO donné le 24/07/2026). La mesure est officiellement fiable ; recalibrage possible à tout moment via bump de `LIDAR_VERSION`.
- [x] **Phase 3 — pans dessinés sur l'ortho** : à l'ouverture de la fiche d'une maison mesurée (avant prospection OU point posé), chaque pan s'affiche en polygone coloré semi-transparent sur la photo avec sa pastille « XX m² » (Geist Mono, bord teinté). Contours vectorisés au moment de la mesure (traçage de frontière + Douglas-Peucker adapté aux anneaux fermés — bug du DP fermé attrapé par test unitaire `tools/lidar-spike/test-outline.mjs`), stockés dans `toit_lidar_pans`, dessinés sans chevauchement. `LIDAR_VERSION` 1→2 : les maisons déjà mesurées régénèrent leurs contours à la prochaine ouverture de fiche. Puis passes cosmétiques v3 (lissage 1 m) et v4 (enveloppe morphologique pour les ardoises sombres à points épars, garde d'honnêteté < 60 %). Argument de vente : « voilà vos 4 pans, 137 m² » sur la tablette, devant la porte.
- [x] **Audit complet + durcissement (post-G0, juillet 2026)** : revue de code calcul + visuel + câblage (rapport dans le journal du SOP). Livré : tests **vitest sur le code shippé** (cœur pur extrait dans `lidar-core.ts`, banc synthétique seedé — `npm run test` dans `web/`) ; bâtiment le plus proche (≤ 10 m) au lieu d'un voisin WFS arbitraire ; **cache LiDAR partagé à l'équipe** (RPC security definer, migration `db/0009` exécutée) ; la re-mesure fraîche prime sur le cache périmé ; timeouts réseau (20 s/requête, 60 s/mesure → `error` re-tentable) ; verdict `grand_batiment` sans télécharger 2-3 Mo ; `toit_lidar_pans` sorti du SELECT global + `fetchPoints` paginé ; `azimut_deg` corrigé en azimut boussole (`LIDAR_VERSION` 4→5, re-mesure paresseuse). Hors périmètre assumé : Web Worker (si jank constaté sur le terrain).
- [x] **Maquette 3D du toit dans la fiche (fin juillet 2026)** : bouton « Voir le toit en 3D » (fiche point + fiche avant prospection) → toit du client manipulable au doigt, three.js en chunk séparé chargé au tap. En parallèle : **carte 3D (extrusion) retirée**. **v2** (rotation réparée, pans jointifs) puis **v3 après le 2e retour captures briac** : silhouette **ancrée sur le polygone BD TOPO** (gouttières droites, angles vrais — mêmes contours rectilignes sur l'ortho), murs droits extrudés de l'emprise avec pignons automatiques, débord de toit réel + ombre sur les murs, marches aux annexes basses, bandeau de rive, lumière maquette. **v4 (finition, 3e série de captures)** : échardes absorbées, noues aimantées aux coins des L, garde de silhouette, murs = hauteur BD TOPO − comble mesuré (fini les « donjons »), fentes comblées, pastilles triées. `LIDAR_VERSION` 9 (re-mesure paresseuse). Avis du chef des ventes à recueillir sur l'effet en rendez-vous.
- [x] **Niveau 3 : pans cochables dans la maquette 3D (fin juillet 2026)** : tap sur un pan (3D, pastille ou légende) pour l'inclure/exclure, total « Σ m² » en direct — le commercial ajuste devant le client ce que la géométrie ne peut pas deviner (extension à refaire avec le reste, véranda à exclure…). Présélection = « la maison » (drapeau `maison` par pan, `LIDAR_VERSION` 17). Sélection non persistée (session) — à mémoriser sur le point si l'usage le demande.
- [x] **v14-v15 : les grandes maisons de lotissement (2e-3e séries de captures briac)** : faces coplanaires d'ailes distinctes séparées en pans propres (`splitDisjointPans`), régions mono-bloc, arcs choisis par longueur, garde des boucles sans jonction. Banc : **22/23 maisons réelles propres** après la v16 (chaînes de 2 coins : riverain lu sur les cellules de l'arête — Danton 130 réparée ; seul résidu : un pseudo-voile bénin sur un pan plat). `LIDAR_VERSION` 16.
- [x] **Campagne d'audit multi-agents + v13 (fin juillet 2026)** : banc d'invariants géométriques permanent + 21 fixtures réelles (dont les 4 étalons des captures briac), non-régression Lyon verte (mesure intacte depuis le Gate G1, toits plats même améliorés), 4 familles de défauts 3D corrigées (voiles, corps coupé, escaliers, croisements) — 15/21 maisons parfaitement propres au banc, résidus bénins. `LIDAR_VERSION` 13.
- [x] **Confrontation factures + badge « la maison » (fin juillet 2026)** : autopsie des 2 chantiers du chef des ventes (`tools/lidar-spike/autopsy.mjs`) — Rosa Floch : corps principal mesuré **219 m² vs 223 facturés (−1,8 %)**, l'écart apparent (+36 %) venait du total qui incluait annexe plate et extension ; Deschard : sous-mesure réelle −15 % (pan mal ajusté + toit éclaté en 2 polygones BD TOPO + chutes facturées), à creuser. **Demande du chef des ventes actée** : le badge affiche désormais LA MAISON (corps principal par connectivité — les décrochés coupent, extensions/annexes/garages exclus), plus le grand total. `LIDAR_VERSION` 10.
- [x] **Reprise + revue + veille multi-agents (fin juillet 2026)** : état vérifié (31 tests, build, banc 22/23), revue critique des 4 modules (5 correctifs, dont la sélection niveau 3 périmée à l'arrivée d'une nouvelle mesure et l'écart badge/Σ), campagne de veille 5 agents (`etude-veille-lidar.md`, vérifications en direct sur les API — aucune menace sur nos flux).
- [x] **Paliers 1-3 de la veille (fin juillet 2026)** — 9 commits :
  - **Viewer** : rendu à la demande + ombre figée (batterie en RDV), robustesse contexte WebGL iOS, finitions tactiles ;
  - **v18 verdicts parlants** (motifs des no_data, % végétation, secours classe 67, classification de dalle, `grand_batiment` croisé logements/étages/RNB) — **migration `db/0011` À EXÉCUTER** ;
  - **v19 longueurs par type d'arête** (faîtage/arêtiers/noues/rives/égouts/solins — le chiffrage couvreur au mètre) + badge = Σ + chutes suggérées (% à valider factures) ;
  - **v20 géométrie d'entrée** : découpage RNB des polygones fusionnés, annexes même-parcelle incluses (piste Deschard), éclaireur MNH ;
  - **cache COPC par dalle** (transfert ÷5-10 dès la 2e maison du quartier) ;
  - **fiche** : attributs BD TOPO enfin lus (**migration `db/0010` À EXÉCUTER**), plan coté SVG, **rapport client** imprimable/partageable, avant/après matériaux (ardoise/tuile/zinc).
- [ ] ⬜ **Sondes terrain post-v20** : re-taper Deschard (annexe même-parcelle), une bande fusionnée (découpage RNB), une maison sous canopée (motif) — les fixtures ne couvrent pas la collecte.
- [ ] ⬜ **Quick wins estimation actuelle** (« étage 1 », ½ j, à valider) : débords de toit, toits plats, formes en L — améliore le fallback partout où le LiDAR ne peut rien.

## Retours terrain briac (fin juillet 2026)
- [x] **Filtre carte par ancienneté** (voir section Fiabilité carte, migration `db/0012`).
- [x] **RDV à la demi-heure** : le `datetime-local` (roue des 60 minutes, step ignoré par iOS) remplacé par date native + sélecteur de créneaux 7 h → 21 h (:00/:30) — un seul tap.
- [x] **Agenda : vue « Clients »** : segmented Agenda/Clients en tête d'onglet ; une ligne par client (regroupé par nom, repli adresse — RDV le plus pertinent : prochain à venir, sinon dernier passé), tap → fiche client complète (RDV + badge statut, téléphone cliquable, adresse, notes, badges maison, **maquette 3D, plan coté, rapport client** — mêmes briques que la fiche carte, backfill de mesure automatique), boutons Carte / Modifier le RDV.

## Audit anti-bugs de toute l'app (fin juillet 2026, demande briac)
- [x] **Campagne multi-agents** : 6 auditeurs (carte, fiches/3D, agenda, couche données, UI mobile, parcours transverses) + un contre-expert sceptique par bug — **49 bugs confirmés, 7 réfutés**. Corrigés en 3 commits :
  - **Sheets qui ne défilaient pas au doigt** (bug déclencheur de briac : conflit geste vaul vs scroll natif) — corps des 4 sheets en `data-vaul-no-drag` ;
  - **vague 1** : suppressions honnêtes (RLS = 0 ligne sans erreur → faux « supprimé »), issues de RDV anti-double-tap + erreurs remontées, bandeau d'échec agenda, formulaire RDV en diff (fin de l'écrasement des modifs concurrentes), pagination RDV + stats (troncature silencieuse à 1 000 lignes), resynchronisation realtime après veille iOS (refetch fusionnant + visibilitychange), éditions pendant la pose rejouées en base, mois précédent correct en fin de mois, jours en heure locale, retry du journal des visites ;
  - **vague 2** : fiche client non modale (3D plein écran et rapport étaient inertes — bloquant), pose refusée si profil non chargé (les points partaient en mode local silencieux — bloquant), verdict LiDAR `error` jamais persisté (il écrasait une mesure valide), fiche point : réouverture propre + diff contre l'instantané initial, garde visée/onglet/filtres à la pose, badges clusters par position, rechargement auto sur chunk mort post-déploiement (`vite:preloadError`), Imprimer masqué en PWA iOS installée.
- [ ] ⬜ **Résidus assumés (mineurs)** : `fetchRevisits`/`fetchRelances` non paginés (il faudrait > 1 000 « à revoir » simultanés) ; pastille de note divergente si la RLS refuse la dénormalisation (logué) ; journal des visites côté client (un trigger SQL `point_events` serait la solution transactionnelle — chantier dédié) ; échecs de chargement Accueil silencieux (sections vides).
- [x] **Contre-audit de régression (24/07/2026, multi-agents)** : les correctifs de l'audit avaient eux-mêmes introduit des régressions (agenda blanc corrigé en `3fa0a10`, scroll des sheets) → 8 chercheurs (4 en relecture des commits `191037f..a0630ed`, 4 en audit UI mobile iOS) + un contre-expert sceptique par bug : **35 confirmés, 4 réfutés**, tous corrigés (commits `ceac5f4..2c57fea`). Les gros morceaux : refetchs concurrents au réveil iOS qui s'écrasaient (jeton de génération + mutations locales bufferisées dans `usePoints`, anti-course Agenda/Stats), pagination sans tie-breaker unique (RDV perdus/dupliqués à la frontière des 1 000), profil jamais re-tenté après un échec (pose bloquée ~1 h) + course au changement de compte, MapView hors du boundary anti-écran-blanc, boucle de reload possible sur `vite:preloadError`, vente doublée dans les stats si la bascule du point échouait, recadrage carte derrière la sheet (hauteur mesurée désormais), rapport client imprimé tronqué à 1 page + sheet imprimée par-dessus, « Supprimer » sans confirmation (2 taps), cibles tactiles < 44 px partout + safe-areas paysage.
- [ ] 🔵 **Chantier scroll/clavier des sheets iOS (en cours, validation terrain briac)** : 4 hypothèses vérifiées et poussées le 24/07 — `92dvh` (viewport réel), `repositionInputs={false}` (vaul déplaçait/rétrécissait la sheet au clavier sans jamais restaurer), `flex-shrink: 0` sur les enfants du corps (compressés au lieu de faire défiler — cause probable du « bas inatteignable »), recollage `scrollTo(0,0)` au blur. À valider sur iPhone : scroll jusqu'aux boutons + saisie d'une note.

## Chantier UX/UI — implémentation (24/07/2026, périmètre validé par briac)
- [x] **Lot A complet (34 quick wins) + B2**, en 13 commits (`598cf81..`) : socle sheets (pied d'action sticky, Supprimer déclassé, module « Toiture mesurée » repliable avec segmented 3D·Plan·Rapport) ; boucle de pose (plus de fiche auto après pose — toast « + Note », statut au-dessus de la 3D + CTA « Poser · {statut} », presets de relance + J+7, formulaire dans l'ordre d'usage, marqueur « Absent » blanc vs « Impossible » sombre, filtres en zone pouce avec badge, pastilles m² agrégées au dézoom, zoom guidé du réticule, « Vu le … ») ; RDV/agenda (fiche client au tap depuis la carte RDV, issues limitées au jour J, prénom au lieu de l'email, « Itinéraire » explicite, bouton Aujourd'hui, recherche + homonymes vue Clients, nom client pré-rempli, dates 1 tap) ; argumentaire (provenance des badges tappable, tilde réservé au douteux, ordre année·DPE·matériau·surface, matériaux « Votre toit en… » remontés, lettres A/B/C et couleurs de pans unifiées 3D/plan/rapport/ortho, « Gouttières », rapport signé) ; manager/accueil (classement commerciaux seuls avec portes + chevron, objectif limité à Semaine + agrégé équipe, stepper d'objectif, graphe étiqueté + jour courant, Info « contact » à la place du TODO, feed cliquable, refetch au réveil + bandeaux d'erreur) ; dialogues système remplacés (2 taps, stepper).
- [x] **Points à trancher : arbitrés (25/07, briac + chef des ventes) et implémentés** : statut **« Hors cible »** (nouveau, migration `db/0013` **exécutée** le 25/07) + « Impossible » renommé **« Refus »** (libellé seul) ; **déplacement d'un point** par appui long → drag au doigt jusqu'à la bonne maison (auteur ou manager, toast Annuler) ; **carte privée** (le commercial ne voit que SES points — croisement de tournées jugé rare —, le manager voit tout + filtre par commercial ; relances Accueil/agenda alignées) ; **agenda partagé** par défaut + chip « Mes RDV », grille du mois colorée par commercial ; **tunnel sans « Contacts »**, « RDV honorés » → « RDV effectués » sur cohorte cohérente (effectués ÷ RDV échus de la période) ; **manager réintégré au classement**. Pas de pose 1-tap ni de pastille relance (refusés).
- [x] **Lot B « terrain » (25/07, arbitrage briac : zéro prospect perdu)** — 3 commits :
  - **B1 bloc « Rendez-vous »** en tête de fiche point : prochain RDV à venir (sinon le dernier + son issue), « Appeler », « Voir dans l'agenda » (bascule d'onglet calée sur le jour du RDV) ; **bandeau ambre « Aucun RDV planifié » + Planifier** sur un point `rdv_pris` orphelin (le trou silencieux) ; « + RDV » à côté du label Client ; le bloc se rafraîchit quand un RDV est posé par-dessus la fiche.
  - **B6 chip « À relancer »** dans les filtres carte (`revisit_at` atteint), combinée en ET, assouplie à la pose. Au passage : défaut `--chip: var(--accent)` (l'état actif des chips sans couleur inline reposait sur une variable indéfinie).
  - **B10 téléphone client sur le point** (migration `db/0014` **exécutée** le 25/07) : champ dans la fiche, `tel:` dans l'en-tête, bouton d'appel sur les relances de l'Accueil, synchro RDV bidirectionnelle (pré-rempli à la création, réécrit seulement si modifié).
- [x] **Lot Manager (25/07, choix briac — l'interface commerciale d'abord, Équipe/test réel en dernier)** — 3 commits :
  - **B8 chevrons ‹ ›** sur les Stats : navigation vers les périodes passées (offset ≤ 0 dans `fetchStatsComparison`/`shiftNow`), delta comparé à la période précédant celle affichée, libellé de plage côté client (réactif au tap), « Aujourd'hui » pour revenir, offset remis à zéro au changement de période.
  - **Pont Stats→Carte** (reliquat B5) : « Voir ses points sur la carte » dans le drill-down manager — bascule Carte avec le filtre « Qui » pré-appliqué et la barre de filtres dépliée.
  - **B14 squelettes** : Stats (KPI/tunnel/classement — finis les faux zéros commentés en réunion) et Accueil (premier chargement seulement).
- [x] **Lot Argumentaire (25/07)** — 2 commits :
  - **B3 sélection Σ → plan + rapport** : l'état `excluded` remonté dans RoofModule (Roof3D contrôlé) — plan coté avec pans exclus grisés (contour pointillé), rapport avec « Σ N m² · surface retenue avec vous » (masquée si égale au badge maison), chutes basées sur la sélection, lignes exclues barrées, partage mentionnant la surface retenue.
  - ~~B13 badge matériau tappable~~ **ANNULÉ après test terrain (25/07, décision briac)** : le select « Toiture constatée » est SUPPRIMÉ de la fiche (il n'ajoutait rien), et l'avant/après matériau 3D (« Votre toit en… ») est SUPPRIMÉ aussi — la maquette s'affiche toujours en mode mesure (une couleur par pan). Le badge matériau redevient informatif (toast) ; `mat_toit_confirme` reste en base pour les anciens points.
- [x] **Lot Agenda/Client (25/07)** — 3 commits :
  - **B12 autocomplétion BAN** sur l'adresse du RDV (helper `searchAddresses` partagé, liste en flux dans la sheet, label normalisé au tap, recherche active seulement au focus). *Reliquat non fait : stocker lng/lat + proposer de lier/poser le point (migration + UX à part).*
  - **B11 fiche client** : issues du RDV du jour (règle jour J + anti-double-tap, statut reflété localement), section « Historique » (tous les RDV du point + journal de notes), module toiture replié.
  - **C1 agenda « jour d'abord »** : mois replié par défaut en bandeau semaine (pastilles par commercial, ambre = relance), tap sur le mois = grille complète, chevrons semaine/mois selon le mode. **→ ANNULÉ le 26/07 (décision briac, testé sur le terrain) : voir « Refonte agenda mois plein écran ».**
- [x] **B4 refonte Accueil (25/07)** : en-tête compact (avatar + Bonjour + rôle), profil/déconnexion dans une sheet derrière l'avatar, carte « Aujourd'hui » (portes/RDV du jour/à relancer + barre d'objectif hebdo, équipe pour le manager), section « Mes RDV aujourd'hui » (heure mono, tap → fiche client, RDV modifiable depuis l'Accueil), état vide explicite.
- [x] **C2 rapport partageable en document (25/07)** : rendu image côté client (Canvas 2D sans dépendance, chunk à la demande `lib/report-image`) — en-tête brandé, chiffres clés (Σ retenue en accent), plan coté redessiné depuis `buildDiagram` (exclus grisés), tableaux, export 2× ; `navigator.share({files})` avec `rapport-toiture-<adresse>.png` + texte d'accompagnement ; replis texte / téléchargement desktop. **→ L'AUDIT UX/UI EST SOLDÉ** (Lot A, B, C — hors constats écartés et B13 annulé).
- [ ] ⬜ **Équipe/invitations + écran de connexion** : volontairement EN DERNIER (décision briac 25/07) — l'interface commerciale doit être parfaite avant de lancer le test réel à plusieurs.

## Prochains chantiers (à choisir avec briac)
- [x] **Retours terrain briac (25/07, post-audit)** : formulaire RDV sans presets de date (« Demain/Après-demain/Samedi » retirés) ; fiche point réorganisée — bloc RDV → Client → maison/toiture → notes → **statut replié** en ligne compacte (« Statut · ● · Modifier »), relance toujours visible sur « à revoir » ; auteur affiché seulement si ≠ soi ; téléphone d'en-tête masqué si le bloc RDV affiche « Appeler » ; bloc RDV en colonne alignée à gauche avec **« Y aller » (Waze)** à la place de « Voir dans l'agenda » (chaîne focusDay retirée) ; badges **« Murs … » et « N étages » supprimés** (« N logements » conservé — alerte collectif) ; pastille « N m² · laser » masquée quand le module Toiture mesurée est affiché dessous ; **typo harmonisée** (mono réservé aux heures/m², en-tête et métas de notes en Geist, échelle 14/13/12) ; **notes sans email** (date seule pour soi, prénom pour un collègue — `firstNameOf`) ; **bloc RDV fond blanc + bordure accent** ; **« Revoir le » remonté dans la section client** (sous nom/téléphone), presets de relance retirés (J+7 auto conservé) ; **module toiture TOUJOURS replié** dans les trois fiches (la 3D ne surgit plus seule quand la mesure tombe, `defaultOpen` supprimé) ; **marqueurs carte redessinés** (rendu 3×, double ombre encre, dégradé vertical + reflet, **sans anneau blanc** — filet hairline seul, « Absent » garde son anneau coloré — sémantique A13 conservée) ; **le vrai marqueur remplace les ronds plats** partout en contexte carte (`markerDataUrl` : sélecteur du viseur, chips de statut fiche + filtres, fantôme de drag) ; **vue Plan RETIRÉE, puis affichage UNIQUE acté** — les alternatives testées le même jour et écartées : « HD » WMS 512 px (aucune différence perçue) puis SAT 2 Esri/Maxar (30-50 cm, trop floue au zoom maison même avec l'astuce retina tileSize 128) ; le bouton de bascule est supprimé, l'ortho IGN (la plus nette gratuite en France) est le seul affichage. Une 2e imagerie ne reviendra que payante/à clé (Google Map Tiles, Mapbox) ; **un point déplacé (drag) recalcule désormais adresse + fiche maison + mesure LiDAR** sur la nouvelle position (les données dérivées des coordonnées étaient figées sur l'ancienne maison — bug retour briac ; les données humaines restent attachées au point).
- [x] **Refonte agenda « mois plein écran » (26/07, décision briac — retour assumé sur C1)** : après test terrain, le « jour d'abord » ne convenait pas — en arrivant sur l'onglet on veut LE MOIS. 3 commits (`511b84c..814a330`) : bandeau semaine et toggle retirés ; la grille du mois occupe **toute la hauteur** de l'écran (semaines d'égale hauteur qui grandissent, scroll seulement si l'écran est trop petit) ; le planning du jour devient une **sheet vaul** (gabarit commun obligatoire) ouverte au tap sur une case — cartes RDV (issues jour J + anti-double-tap intacts), relances ambre, pied « RDV ce jour » (création pré-datée via `defaultAt` du formulaire) ; fiche client / Modifier / Carte **ferment d'abord la sheet du jour** (pas d'empilement de drawers vaul sur iOS). DA : titre du mois Geist 17 à gauche, chevrons + Aujourd'hui groupés à droite, chip « Mes RDV » intégrée à la carte, hairlines entre les semaines (réf. Fantastical), en-tête L-D en Geist (plus de mono sur des lettres), purge du `.form-input` legacy. Garde-fous conservés : agenda partagé + Mes RDV, couleur = commercial dans la grille, anti-course des reloads, realtime + recalage au réveil iOS (porte désormais sur le mois affiché), bandeau d'erreur, vue Clients intacte. **Suite (même jour, retours briac)** : en-tête « Agenda / + RDV » supprimé (le segmented ouvre l'écran, la grille gagne la hauteur) ; **RDV de la sheet du jour en « rail horaire »** (réf. Fantastical) — heure Geist Mono, barre verticale couleur commercial, plus de cadre de carte ni de badge « À venir » (statut affiché seulement quand il est donné), bloc entier tappable → fiche client, prénom du commercial seulement si ≠ soi, notes en citation à filet ; Appeler/Itinéraire + issues jour J restent en 1 tap ; **Modifier/Supprimer/Carte déplacés dans la fiche client** (« Supprimer le RDV » en lien danger discret, confirmation 2 taps, titulaire/manager seulement).
- [x] **Refonte Stats « Bilan hiérarchisé » (26/07, direction choisie par briac)** : en-tête compacté (titre « Statistiques » supprimé, la **période devient le titre** entre les chevrons ‹ ›) ; les 3 cartes KPI égales remplacées par un **chiffre héros** (ventes en 42 px + delta en phrase « +3 vs semaine précédente », portes/RDV/conversion en ligne secondaire — fini le doublon avec le tunnel) ; « Ma position » du commercial et le pont « Voir ses points sur la carte » intégrés au héros ; **tunnel en barres proportionnelles** (largeur = volume, taux de passage sous chaque barre, fuite la plus forte marquée « point de blocage » sur son taux, ventes en vert plein — définitions actées inchangées) ; graphe d'activité hiérarchisé (jours estompés, aujourd'hui plein). Garde-fous conservés : anti-course des fetchs, squelettes, bandeau d'erreur, chevrons B8 + « Aujourd'hui », drill-down manager + stepper d'objectif, classement complet manager compris.
- [x] **Refonte Accueil : feed remplacé par le Guide (26/07, arbitré avec briac)** : « Activité récente » supprimée (`fetchRecentActivity` purgé — un fetch de moins) ; l'Accueil garde Aujourd'hui + Mes RDV + À relancer et gagne une section **« Guide de l'app »** : 3 tutos pas-à-pas (Poser un point · Prendre un RDV et le solder · La fiche maison & le toit 3D), cartes horizontales → sheet vaul (capture + texte par étape, Précédent/Suivant, points de progression). Tutos resserrés le même jour (retour briac : trop d'étapes) à **3 étapes d'une phrase chacun**. Visuels = **captures prises par Claude via Playwright** (`tools/screenshots/` : `shoot.mjs` moisson connectée en lecture seule — jamais Poser/Enregistrer/issues —, `convert.mjs` PNG→WebP 780 px, identifiants `GUIDE_EMAIL/GUIDE_PASSWORD` dans `web/.env` non versionné) : 8/9 en place (`web/public/guide/*.webp`, hors precache PWA), **manque `pose-3` (drag)** = mutation réelle, capture iPhone à faire par briac. Rejouable à chaque évolution d'UI. Bonus des captures : **bug d'attribution corrigé** (le pill « © IGN » MapLibre perçait le `visibility:hidden` du map-slot et flottait sur l'Agenda/Accueil). Reste ouvert (décidé) : déclenchement auto du 1er tuto à la première connexion — à faire avec le chantier Équipe.
- [x] **Nouvelle DA « Encre & signal » (26/07, choisie par briac sur prototypes comparés)** : méthode = 3 variantes de tokens (cuivre/serif « artisan », orange sécurité, vert laser) **injectées par Playwright sur les vrais écrans** (`tools/screenshots/da-shots.mjs`, planches `screenshoots/da/compare--*.png`) → briac a tranché pour **Orange sécurité** : encre quasi noire #111113, papier neutre, **relief marqué** (ombres franches), **accent unique orange signal #f54e00 réservé aux actions/liens/états actifs — jamais pour de la donnée**. Vert laser écarté (collision avec le vert « Vendu »). Implémentée en 3 commits : tokens `index.css` + CLAUDE.md (nouvelle loi DA), DA cuite en TS basculée (ACCENT MapView — halos/réticule/maison surlignée —, badge clusters, rapport client canvas, gris des pans exclus), captures du guide régénérées en une commande. Les couleurs de **statuts** et la palette des **pans** n'ont pas bougé (sémantique métier ≠ marque) ; « RDV pris » reste bleu, désormais bien distinct de l'accent. À surveiller sur le terrain : orange signal vs ambre « À revoir » (familles proches — décaler l'ambre si confusion). **Suite (même jour) — tunnel simplifié (retour briac « incompréhensible »)** : 4 volumes (Portes · RDV pris · Effectués · Ventes) et **2 taux seulement — effectués ÷ pris et ventes ÷ effectués** (libellés de taux, taux portes→RDV et « absents » supprimés) ; **⚠ le taux « effectués ÷ pris » remplace À L'AFFICHAGE la cohorte du 25/07** (choix briac : taux métier simple, peut dépasser 100 % sur une petite période — `rdv_planifies` reste calculé en base si on y revient) ; taux le plus faible en rouge sans texte (compte dès que son dénominateur existe — 0 % est un vrai blocage) ; delta du héros seulement s'il est non nul ; conversion au pied du tunnel uniquement (doublon héros supprimé). **Suite (même jour) — doctrine typo (bilan briac « trop de polices »)** : 2 familles installées mais 4 textures perçues (le mono majuscule `.eyebrow` sur chaque titre de section) et 19 tailles de 9 à 42 px dont 4 demi-pixels. Corrigé en 3 commits : **Geist partout** (labels de section, user-role, jours du graphe), **mono strictement réservé aux chiffres** (dans les lignes mixtes, seuls les nombres restent en tnum), **échelle fermée de 8 crans** (10 micro-grille · 11 · 12 · 13 · 15 · 17 · 24 · 42, graisses 550/700 purgées). Doctrine gravée dans CLAUDE.md.
- [x] **CHANTIER GUIDES v2 (27/07, session dédiée — plan complet : `docs/plan-guides-v2.md`)** : la v1 (« totalement mauvaise », briac) est remplacée. **(1) Audit** : `tools/screenshots/audit-guide.mjs` (nouveau, lecture seule) rejoue chaque tuto étape par étape — 8 des 9 paires texte/capture étaient en décalage (réticule sur une pelouse, fiche « Absent » pour illustrer client/relance, formulaire ouvert du mauvais écran, issue déjà soldée, badges promis absents…) ; cause racine : écrans moissonnés tels quels au lieu de capturés d'après le texte. **(2) Plan éditorial** (sous-agent, claims vérifiés dans le code, validé briac — 4 décisions tranchées : drag hors guides, halo orange sur les petites cibles, 4 guides, données semées) : **4 guides = la boucle terrain** — Poser un point (3) · **Relancer une porte (2, nouveau)** · Prendre un RDV (3) · Mesurer un toit (3), une phrase par étape, la capture montre LITTÉRALEMENT la phrase. **(3) Implémenté** : `Guide.tsx` réécrit (textes v2, `?v=2` casse le cache des anciennes images) ; `shoot.mjs` v2 (adresses figées par capture, retry BAN, barre de recherche vidée + blur avant chaque shot, halo orange injecté sur l'élément réel, préconditions vérifiées → capture SAUTÉE + PNG périmé purgé si les données manquent) ; `convert.mjs` recadre chaque capture sur sa bande utile (les pleins écrans v1 étaient illisibles dans le cadre ~carré ; NB sheet ouverte → page capturée 664 pt, pas 844) ; **bug réel corrigé au passage** : l'heure « 11:00 » cassait sur 2 lignes dans la sheet du jour (colonne 44→48 px). **8/11 captures en place** (pose-1/2/3, relance-1, rdv-2, maison-1/2/3 — dont pan 3D exclu au tap + rapport « Σ retenue » cohérents entre eux). **Reste (semis briac, ~15 min, § Préparation du plan)** : renommer le profil de capture (l'email s'affiche sinon dans le rapport — recadré hors champ en attendant), 1 point « RDV pris » sans RDV (→ `rdv-1`, passer `RDV1_ADDR`), 1 RDV « à venir » daté du jour (→ `rdv-3`), 1 relance échue (→ `relance-2`), RDV du mois depuis 2 comptes avec `client_name` partout (→ re-shoot `rdv-2` : aujourd'hui toutes les pilules sont bleues et une affiche une adresse) ; puis `node shoot.mjs && node convert.mjs`. Déclenchement auto du 1er tuto à la 1re connexion : toujours prévu avec le chantier Équipe.
- [x] **Géolocalisation vivante (27/07, retour terrain de l'ami)** : le point de position ne suivait pas le commercial — le `GeolocateControl` MapLibre était en one-shot volontaire (son mode suivi volait la caméra après chaque recherche d'adresse, bug du 25/07 : point et caméra y sont couplés). Remplacé par un **`watchPosition` qui alimente le point en continu** (source + halo/point bleu liseré blanc, AU-DESSUS des marqueurs) pendant que **la caméra ne bouge jamais toute seule** ; le bouton ⌖ (contrôle custom, même emplacement/styles) **recentre** simplement sur le point vivant (zoom ≥ 17), et relance/explique si la localisation est refusée. GPS coupé hors de l'onglet Carte et quand la PWA passe en arrière-plan (`visibilitychange`), recalage au premier fix du retour ; le zoom d'accueil sur le 1er fix garde sa garde « caméra intouchée ». Vérifié par sonde Playwright (`tools/screenshots/probe-geoloc.mjs` : GPS simulé A→B — le point bouge, la carte non ; ⌖ recentre). À confirmer par l'ami en vraie tournée (précision/latence iPhone).
- [x] **Vue « Contacts » (27/07, demande briac — remplace la vue « Clients » de l'agenda)** : l'ancienne vue regroupait les RENDEZ-VOUS par nom (un « À revoir » sans RDV n'y figurait jamais, un client vendu y restait) ; la nouvelle liste les PROSPECTS = les **points** « RDV pris » + « À revoir » (datés ou non) — vendre ou annuler fait sortir le contact par le changement de statut, en direct (l'abonnement realtime points existait déjà). Décisions briac : **contacts propres à chacun** (le manager voit tout — même règle que les relances), **tri par échéance** (prochain RDV ou date de relance, la plus proche d'abord ; sans échéance ensuite par dernière visite), **chips de filtre** « RDV pris » / « À revoir » (re-tap = tous), **date du prochain RDV affichée** (colonne de droite + fiche). Tap → nouvelle **`ContactSheet`** construite sur le point (`ClientSheet` reste la fiche du flux RDV depuis le planning du jour) : échéance + badge statut, téléphone (`tel:`), adresse (**Waze**), journal de notes, badges maison + « Toiture mesurée » repliée (backfill LiDAR paresseux), pied « Voir sur la carte ». Vérifié par sonde (`tools/screenshots/probe-contacts.mjs`) : 6 contacts sur les données réelles, Dylan (vendu) exclu, ordre d'échéance exact, filtre et fiche OK.
- [x] **Saisie manuelle d'un contact (27/07, demande briac)** : bouton « + » accent à droite de la recherche de la vue Contacts → sheet « Nouveau contact » (adresse **BAN obligatoire** — la suggestion choisie donne les coordonnées, texte retouché = choix invalidé —, chips statut RDV pris/À revoir avec les vrais marqueurs, nom/téléphone/note, « Revoir le » J+7 si À revoir). La création passe par **la même chaîne que la pose carte** (`insertPoint` : point + journal `point_events` → compté dans les stats comme une porte — assumé, question n° 27 posée à l'ami —, adresse inverse, fiche maison, mesure LiDAR en fond) puis `updatePoint` pour les infos client ; pour un « RDV pris », **date + heure (créneaux 7 h-21 h) directement dans le formulaire** et le RDV lié se crée dans la même passe (retour briac le jour même : le 1er jet enchaînait sur le formulaire RDV complet, un second modal qui redemandait ce qui venait d'être saisi — supprimé ; si la création du RDV échoue, la fiche du point propose « Planifier », filet anti-« trou silencieux » existant ; la note reste sur le point, l'agenda l'affiche en contexte). **Garde anti-doublon** : refus si un point existe à l'adresse exacte (tout statut), toast avec statut existant + action « Voir » (décision briac : adresse exacte, pas de rayon). Position = point BAN (souvent la parcelle, pas pile le toit) : l'appui long + glisser corrige, le toast de création propose « Voir sur la carte ». Sonde lecture seule `probe-contact-form.mjs` (autocomplétion + garde anti-doublon vérifiés sans écriture) ; le chemin de création réelle reste à valider par briac (créer puis supprimer un contact test).
- [x] **Décaler un RDV depuis le contact et la carte (29/07, demande briac « le client n'est plus dispo »)** : le seul chemin était Agenda → jour → RDV → fiche client → « Modifier le RDV » — il fallait CONNAÎTRE la date pour retrouver le RDV, et les deux entrées naturelles étaient des impasses. Corrigé : **fiche contact** — « Modifier » (crayon) en bout de ligne d'échéance quand un RDV à venir existe (ferme la sheet puis rouvre le formulaire en édition, circuit `editing` existant de l'agenda) ; **fiche du point (carte)** — « Modifier » dans le bloc Rendez-vous (`onRdvNeeded` étendu : 2e argument = RDV à éditer, `MapView` monte `AppointmentForm` avec `existing`). **Piège de duplication corrigé au passage** : le bouton « RDV » de la ligne Client s'affichait même avec un RDV à venir et ouvrait la CRÉATION (→ 2e RDV, l'ancien restait « à venir ») — il est désormais masqué dans ce cas, le décalage passe par « Modifier » ; un RDV « à venir » à date passée reste modifiable (on décale un RDV oublié, on n'en recrée pas). Sonde `probe-rdv-edit.mjs` (lecture seule, Annuler jamais Enregistrer) : les deux chemins ouvrent « Modifier le RDV » pré-rempli, bouton doublon absent.
- [x] **« Y aller » sur la fiche maison avant prospection (29/07, demande briac)** : naviguer vers une maison repérée sur la carte SANS avoir posé de point. Bouton `btn-ghost` à gauche de « Poser · … » (gabarit deux boutons des autres fiches), lien `wazeUrl` avec les **coordonnées de la maison tapée** (nouvelle prop `coords` passée par `MapView` — les coordonnées priment sur l'adresse texte, règle du 25/07). Au passage : `text-decoration: none` sur `.btn` (premier `<a>` à porter la classe, il s'affichait souligné). Vérifié par sonde lecture seule (lien `waze.com/ul?ll=…` aux coordonnées exactes).
- [x] **7e statut « Ancien client » (29/07, demande de l'ami)** : maison déjà vendue AVANT la prospection — référence de rue, à ne pas re-démarcher comme un inconnu. Suit le rail de « Hors cible » : migration `db/0015` (**à exécuter dans le SQL Editor Supabase — pas encore fait au 29/07**), 7e entrée `status.ts` (**teal #0e9384**, choisi à distance du vert « Vendu »), glyphe **étoile pleine** dans `markers.ts`, token `--st-ancien-client` ; chips visée/fiches/filtres suivent automatiquement. Décisions briac : **section Client étendue** au statut (nom + téléphone + note) ; **intégré à la vue Contacts** avec une 3e chip de filtre — sans échéance, les anciens clients se rangent naturellement en fin de liste ; **saisie manuelle** possible (3e chip du formulaire « + », ni RDV ni relance). Stats : une pose compte une porte, **jamais une vente** (vérifié `data/stats.ts:133`) — Q27 complétée (ressaisie en masse) + **Q28 ajoutée** (anciens clients = actif d'agence ou du commercial ? contacts « chacun les siens » aujourd'hui). **Garde-fou vérifié à la sonde** : tant que la migration n'est pas passée, Postgres rejette l'enum inconnu et vidait TOUTE la liste Contacts — `fetchContacts` se replie sur les statuts sûrs (warn console), l'ordre migration/déploiement est indifférent. CLAUDE.md corrigé (« 5 statuts figés » → 7 + mode d'emploi d'ajout). **Suite (même jour, retour briac après test)** : poser un « Ancien client » **ouvre la fiche du point d'office** (section Client + note — le statut n'a aucun intérêt sans ses infos) ; 2e exception au régime « toast seul » de l'audit A1, à côté de « RDV pris »→formulaire ; le toast garde « Annuler », « + Note » redondant retiré pour ce statut.
- [x] **Stats : section « Points posés » (29/07, demande briac)** : sous « Portes toquées par jour », une ligne par statut posé sur la période — pastille + libellé, **barre proportionnelle à la couleur sémantique du statut** (jamais l'accent), compte en mono à droite ; les statuts à zéro sont masqués (ordre stable de la palette pour comparer les périodes), état vide explicite. Données : `parStatut` ajouté à `CommercialStats` (bump par commercial ET équipe dans la boucle d'événements existante — la somme = `portes`) → **le drill-down manager et les chevrons de période marchent sans code supplémentaire**. `check-stats.mjs` capture désormais aussi le bas de l'écran (vérifié : la répartition colle au tunnel, Ancien client teal compté en porte, pas en vente).
- [x] **Mode sombre optionnel (29/07, chantier briac — 3 étapes, validation entre chacune)** : prototypes injectés sur les VRAIS écrans (`da-shots.mjs` étendu : écrans carte + fiche ouverte — le marqueur est repéré par ses pixels ambre dans la capture puis cliqué, le balayage aveugle dérivait — et filtre de variantes en argument) ; planches clair | sombre A « Étagé » | sombre B « Ligné » → briac a tranché **« Ligné »** (surfaces au ras du fond, relief par BORDURES : hairlines contrastées + anneau 1 px intégré aux tokens d'ombre — les ombres franches du clair ne portent pas sur fond nuit). Implémentation : tokens `[data-theme="dark"]` (index.css), **clair par défaut** — attribut absent et **0 pixel ne bouge en clair** (diff strict des sondes avant/après) ; sélecteur **Clair · Sombre · Auto** dans la sheet de profil (localStorage, `lib/theme.ts`, « Auto » suit le téléphone à chaud), application AVANT le premier rendu (script inline index.html — pas de flash blanc), meta theme-color suivie, Toaster Sonner câblé au thème effectif. Boutons encre inversée en **blanc pur** (retour briac : le presque-blanc lisait « gris »). Statuts : teintes inchangées, « Refus » (1,6:1, illisible) et « Hors cible » **remontés en sombre seulement**. Balayage des couleurs cuites hors CSS : marqueurs/clusters/halos/réticule MapView **inchangés** (ils vivent sur la photo), contrôles + attribution MapLibre et barre de recherche déjà en tokens ; **pastilles m² blanches passées à l'encre littérale** #111113 (chiffres blanc sur blanc en sombre — retour briac), halo des cotes du plan en nuit, **rapport client resté PAPIER** (tokens clairs re-déclarés sur le sous-arbre, boutons encre restaurés) ; captures du Guide claires dans leur cadre (assumé) ; manifest PWA laissé clair (le runtime suit la meta). **Roue dentée « Réglages »** dans l'en-tête Accueil (retour briac : l'avatar seul n'était ni visible ni intuitif), sheet renommée « Profil & réglages ». Sondes : `THEME=dark` sur check-accueil/check-stats, `probe-theme.mjs` (bascule + persistance), `probe-roof-sombre/clair.mjs` (module toiture).
- [x] **Fusion « Vendu » + « Ancien client » → statut « Client » (29/07 soir, retour du chef des ventes)** : un seul statut à l'écran — l'état dit « cette maison est un client de l'agence », la vente redevient un ÉVÉNEMENT. **Merge à l'affichage, enum intact** (zéro migration, stats passées intactes, réversible) : les deux valeurs restent en base et partagent libellé/vert #17b26a/glyphe ✓ (`DISPLAY_STATUSES` = une seule chip « Client » qui écrit `ancien_client`, `sameDisplayStatus` pour chips actives et filtres, la chip du filtre carte bascule les deux valeurs ensemble). **Seule l'issue RDV « Vendu » écrit `vendu`** et compte une vente au tunnel ; pose directe, bascule manuelle (re-taper « Client » sur un point déjà client ne réécrit pas la valeur — le journal garde vente vs ancien client) et saisie manuelle Contacts écrivent `ancien_client` (une porte, jamais une vente) — vente conclue hors RDV non comptée (assumé briac). Décisions briac : **vert succès conservé** (l'étoile teal disparaît, le ✓ reste), **les clients restent dans Contacts** (« vendre fait sortir le contact » ne vaut plus — SAV/parrainage/référence de rue, `fetchContacts` inclut `vendu`), **une seule ligne « Client »** dans « Points posés » (somme des deux valeurs), Q28 (actif d'agence ou du commercial ?) reportée — « chacun les siens » pour l'instant. Vérifié : build + sonde contacts (12 contacts, le vendu Dylan inclus, filtre « Client » matche les deux valeurs, badge de fiche « Client ») + stats (« Client · 2 » là où s'affichaient Vendu 1 et Ancien client 1, tunnel et classement toujours à 1 vente). Q27/Q28 et CLAUDE.md reformulés.
- [x] **Tâches d'agenda (29/07 soir, demande du chef des ventes via briac)** : entrées libres du planning — « aller chercher l'acompte », « récupérer le panneau » — datées, client/téléphone/adresse facultatifs, note-titre « Quoi faire » (obligatoire), SANS point sur la carte et SANS issues. **Même table que les RDV** (migration **`db/0016` À EXÉCUTER dans Supabase** : colonne `kind` `rdv`/`tache` ; `point_id` était déjà nullable) : mois, temps réel, « Mes RDV », couleur = commercial, édition — tout est partagé gratuitement. Création : chip accent **« + Tâche »** à droite de « Mes RDV » (1 tap depuis le mois, pré-réglée sur la prochaine heure ronde — retour briac le jour même) et bouton **« Tâche »** au pied de la sheet du jour (pré-datée sur ce jour) ; dans la grille du mois et le rail horaire, **l'objet est le titre** (icône presse-papiers, pas de citation redondante) ; le tap ouvre directement l'édition (pas de fiche client) ; **suppression 2 taps dans le formulaire** (titulaire/manager — le « Supprimer » des RDV vit dans la fiche client, qu'une tâche n'a pas). **Hors stats** (décisions briac) : `kind='tache'` exclu du tunnel (fetchStats) et du compteur « RDV du jour » de l'Accueil, mais PRÉSENT dans la liste « Mes RDV aujourd'hui » (le plan de journée). Robustesse : fetch/insert/update rejouent sans la colonne tant que la 0016 n'est pas passée (RDV intacts, seule la création de tâche l'exige — jamais dégradée en RDV silencieux). Sonde lecture seule `probe-tache.mjs` (deux thèmes) ; création réelle validée par briac (migration exécutée, tâche test créée/supprimée).
- [x] **Boucle « Fait ✓ » des tâches (29/07 soir, question briac « je dois la supprimer ? »)** : rien à supprimer — un toggle **« Fait »** sur la tâche (sheet du jour + Accueil, teal `effectue` : la colonne `status` existante, zéro migration), re-tap = remise à faire, aucun impact stats (le filtre porte sur `kind`). Tâche faite : titre barré, pilule du mois estompée. **Les tâches passées NON faites remontent en tête de « Mes RDV aujourd'hui »** sur l'Accueil (date compacte + mention ambre « en retard », bouton ✓ en bout de ligne) jusqu'à être faites ou supprimées — le trou silencieux de l'acompte oublié est fermé. Vérifié par interception réseau Playwright (tâches factices injectées dans la réponse REST, zéro écriture) : retard en tête + ✓, faite barrée + « Fait » rempli, RDV intacts.
- [x] **Fusion des fiches RDV → fiche PROSPECT unique (29/07 soir, audit demandé par briac : « plein de modals différents »)** : l'audit des 4 portes d'entrée (Accueil · planning du jour · Contacts · carte) montrait 3 fiches différentes (ClientSheet / ContactSheet / PointDetailSheet), les issues Vendu/Effectué/Manqué/Annulé disponibles sur 2 surfaces sur 4 — **impossible de solder depuis la carte**, pourtant LE moment naturel (on sort de chez le client, la carte est ouverte) —, trois habillages du même geste (Y aller / Itinéraire / icône Waze) et deux vocabulaires de badge (statut du RDV vs statut du point). Corrigé : **`ProspectSheet`** (fusion ClientSheet + ContactSheet : tél, adresse Waze, journal, historique des RDV, badges maison, toiture 3D — désormais identiques partout, Contacts gagne l'historique) ouverte depuis l'Accueil, le planning du jour et Contacts ; **`RdvSection`** = section RDV STANDARD (date + issue du RDV, **boutons d'issue dès le jour J**, Appeler / Y aller sur la carte, Modifier, bandeau « Aucun RDV planifié » + Planifier — affiché même quand un ancien RDV soldé est visible) montée dans la fiche prospect ET la fiche du point de la carte → **on solde depuis n'importe quelle porte, carte comprise**. Décisions briac : le rail du jour garde ses issues inline (flux express), badge au **vocabulaire du POINT** (l'issue du RDV en second), « Planifier » câblé partout (Accueil et Agenda montent le formulaire lié au point avec ses défauts). `wazeUrl` extrait dans `lib/nav.ts`. Vérifié : sondes probe-contacts et probe-rdv-edit (les deux chemins « Modifier » passent par la section commune) + captures fiches Accueil (RDV pris : bloc + bandeau + Planifier) et Contacts (Client vert, issue « Vendu »). ClientSheet et ContactSheet supprimées.
- [x] **Convergence finale : LA fiche du point partout (29/07 soir, retour briac « le modal de la carte est top »)** : la fiche prospect née de la fusion du même jour est remplacée — taper un RDV (Accueil, planning du jour) ou un contact ouvre désormais **PointDetailSheet, identique strict à la carte**, édition comprise (statut, client, note, relance — des gestes impossibles hors carte jusqu'ici), via l'adaptateur **`PointSheet`** : mutations par la couche data + rechargement du parent (la carte garde ses mises à jour optimistes `usePoints`, le temps réel raccorde les deux), formulaire RDV monté PAR-DESSUS la fiche comme sur la carte (`apptsVersion`). Rapatrié pour ne rien perdre de l'ex-fiche prospect : section **« RDV passés »** (dates + issues) dans la fiche, **adresse cliquable → Waze** dans l'en-tête (carte comprise — « y aller » est LE geste hors carte), bouton **« Carte »** au pied quand on n'y est pas, et la **suppression d'un RDV déménage dans « Modifier le RDV »** (comme les tâches — un seul endroit, l'ex-bouton de la fiche client disparaît). RDV libre sans point (créé à la main dans l'agenda) → édition directe du RDV. `ProspectSheet` supprimée après une heure de vie — la brique durable de la fusion reste **`RdvSection`** (issues partout, carte comprise). Sondes : probe-rdv-edit adaptée (la fiche reste ouverte sous le formulaire, « Carte » au pied) — les deux chemins « Modifier » repassent ; probe-contacts OK (fiche : tél, Waze ×2, toiture).
- [x] **Refonte des issues de RDV : chaque issue a une conséquence (29/07 soir, audit UX avec briac « si je mets Effectué il ne se passe rien »)** : le diagnostic — sauf « Vendu », une issue ne changeait QUE le RDV, jamais le point : petit badge, maison toujours « RDV pris » sur la carte, et le bandeau « Aucun RDV planifié » vécu comme un reproche juste après avoir soldé. Corrigé, **une issue = une réalité terrain + une bascule du point** (`setAppointmentOutcome` généralisé, visite journalisée = une porte, comme Vendu déjà) : **Vendu** → Client (vente) ; **En attente** (ex-« Effectué », renommé — le prospect réfléchit) → **À revoir + relance J+7** (le système de relances de l'Accueil prend le relais) ; **Refus** (nouvelle issue, **migration `db/0017` À EXÉCUTER**) → point Refus, sans confirmation (choix briac) ; **Annulé** → rien + bouton **« Replanifier »** (rail du jour et bandeau de la fiche — nouveau RDV pré-rempli, l'annulé reste dans l'historique). **« Manqué » retiré des boutons** (client absent = annulé, on replanifie — valeur conservée pour l'historique). **Vente par bascule** : « RDV pris » → « Client » à la main écrit `vendu` (compte au tunnel) + synchronise le RDV « à venir » lié en « Vendu » — la vente est comptée par les DEUX gestes. Stats : « RDV effectués » = en attente + vendus + **refusés** (RDV tenus) ; **les annulés ne comptent nulle part** (un RDV annulé n'est pas un RDV raté). Toasts de conséquence (« En attente — la maison passe en À revoir, relance dans 7 jours ») ; hors carte, la fiche re-lit son point après une issue (la carte a le realtime). ⚠️ À montrer à l'ami : le tunnel ne distingue plus les lapins (Manqué) — ils se liront dans les annulés-replanifiés. Vérifié à la sonde (RDV factice injecté dans la réponse REST) : rangée Vendu · En attente · Refus · Annulé, libellés « En attente » corrects sur les données réelles, tâches et « Fait ✓ » intacts. **Suite (même soir, test briac : « les pastilles doivent rester visibles après En attente »)** : « En attente » devient un **état OUVERT** — le trou repéré : la vente d'un devis en attente n'aurait JAMAIS été comptée (le point passait « À revoir », et À revoir → Client écrit `ancien_client`, pas une vente). Corrigé : un RDV « En attente » continue de proposer **« Vendu » et « Refus »** (pas Annulé — il a eu lieu), **sans limite de date**, dans le rail ET la fiche (même composant `RdvSection` → même modal via carte, agenda, Accueil, Contacts) ; la réponse se donne sur LE MÊME RDV — vente différée comptée, statut compté une seule fois (il ne fait que progresser). La boucle : relance J+7 → « À relancer » de l'Accueil → fiche → un tap conclut. Hygiène : « Vendu » efface aussi la date de relance (la maison ne traîne pas dans « À relancer » une fois la réponse tombée). Vendu / Refus / Annulé restent des issues fermées. Vérifié en lecture seule sur données réelles (Gerard « En attente » montre Vendu/Refus dans le rail et la fiche).
- [x] **Picker de pose en grille 3×2 (30/07, retour briac)** : les 6 statuts étaient derrière un slider horizontal — deux gestes pour un choix fait 40-60 fois par tournée, la moitié des chips hors champ. `.status-picker` passe de flex + overflow-x à une **grille 3 colonnes × 2 lignes** (chips compactées : centrées, 13 px, padding réduit — cibles conservées), tout visible d'un coup, cellules égales pour la mémoire musculaire du pouce. Un seul composant (`StatusPicker`) → corrigé partout : **viseur de pose (FAB +)** ET **fiche maison avant prospection**. Vérifié aux sondes (captures des deux surfaces).
- [x] **« Conversion » = ventes ÷ RDV effectués (30/07, vérification demandée par briac)** : la stat nommée « Conversion » (pied du tunnel + « conv. » du classement des commerciaux) calculait **ventes ÷ portes** — pas la conversion métier. Corrigée aux deux endroits : **ventes ÷ RDV effectués**, la même définition que le taux affiché au-dessus de la barre « Ventes » du tunnel (celle qu'on pilote). Vérifié à la sonde : 5 ventes / 6 effectués → 83,3 % (l'ancienne formule affichait 22,7 %).
- [ ] ⬜ **Écran de connexion** — dernier écran encore à l'ancien style ; l'habiller avec la DA (Geist/Lucide). Petit, rapide.
- [ ] ⬜ **Équipe (invitations)** — le manager partage un code ; les commerciaux s'inscrivent avec ce code et rejoignent SON agence (aujourd'hui chaque inscription = nouvelle agence isolée). Débloque le test réel à plusieurs. *Dépend un peu de l'ami (Q13-16).*
- [ ] ⬜ **Micro-ajustements métier** — après le retour de l'ami : statuts, flux RDV, définitions de stats (« contact » vs « impossible », etc. — voir `questions-ouvertes.md`).

## Idées / plus tard (hors MVP)
- Vue liste des points (filtres)
- Carnet de contacts / mini-CRM (clients à rappeler, R2)
- Chat / annonces manager
- Mode hors-ligne
- Mesure de toiture
- Multi-agences self-service + facturation
